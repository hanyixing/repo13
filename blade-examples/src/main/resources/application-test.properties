# ============================================================
# Test environment
# Loaded by Blade at runtime via:  java -jar app.jar --app.env=test
# (Blade reads .properties only; the matching .yml is a mirror.)
# ============================================================
app.env=test
app.name=blade-examples (test)
server.port=9001
app.show-banner=false
log.level=INFO

datasource.url=jdbc:mysql://test-db:3306/blade_test
datasource.username=test
datasource.password=test123
